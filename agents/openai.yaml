interface:
  display_name: "Visual UI Director"
  short_description: "参考驱动、视觉判断、评分迭代的跨平台 UI 设计导演"
  brand_color: "#5B5CE2"
  default_prompt: "Use $visual-ui-director to explore references, extract Visual DNA, build and implement the visual system, then critique and iterate rendered UI through Gate C."

policy:
  allow_implicit_invocation: true
