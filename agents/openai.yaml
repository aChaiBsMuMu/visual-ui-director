interface:
  display_name: "Visual UI Director"
  short_description: "参考图驱动的跨平台 UI 视觉设计、规范与实现流程"
  brand_color: "#5B5CE2"
  default_prompt: "Use $visual-ui-director to discover references, define a visual standard, and build this interface."

policy:
  allow_implicit_invocation: true
